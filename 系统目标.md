# 乐师对外教学助手 — 系统目标

两层不要混：

- **系统**：对外讲解 Agent（识图 + 多轮对话 + 校本术语锁定）
- **本期开发**：Web 手动上传图片 → **真实识别** → 中英日介绍 → 可继续对话
- **不是预置演示包**：要直接出成品效果
- **参赛视频 / 三件套**：先不做

原稿《多语言翻译软件》作废。App / 小程序只预留，本期不做。

## 已拍板

| 项 | 结论 |
|---|---|
| 后端 | Python |
| 本期前端 | 仅 Web；手动上传图片（不调用手机摄像头） |
| 本期主路径 | 真识图 + 真生成三语介绍 + 可继续对话 |
| 视觉 | 必须用乐师校徽 / 校名 / 乐师蓝金，见 `brand/视觉规范.md` |
| 视频 | 先不做 |
| 教案对照 | 先不做（含义见第 4 节） |
| 预置假演示 | 不要。没有 Key 就接不上，不能拿一段写死的「这是大佛」冒充成品 |
| App / 小程序 | 架构预留，本期无安装包 |
| 模型 | `gpt-5.6-terra`（OpenAI 兼容 `/v1`，配置在 `.env`） |

## 1. 系统一句话

用户在网页上传一张乐山相关图片（默认场景：乐山大佛）→ Agent **真实识别**景物 → 用校本术语表锁词，先给中 / 英 / 日背景介绍 → 可继续多轮追问。专名以老师审定译法为准，模型不得改写。

作品名称：**乐师对外教学助手（校本术语锁定）**  
赛道：4 · AI 辅助编程与智能体开发  
主题句：融合创新+，智绘乐师新征程

不是翻译网站，也不是无边界聊天机器人。只讲乐师 / 乐山故事。

## 2. 架构

```
[Web 本期 · 乐师视觉]  [App 预留]  [小程序 预留]
              \           |          /
                \     Python API    /
                       |
                Agent 编排（分流 → 装载术语 → 锁定 → 交付）
                       |
           +-----------+-----------+
           |           |           |
        视觉模型     对话模型     术语表 JSON
```

| 层 | 本期做 | 本期不做 |
|---|---|---|
| 前端 | Web：顶栏校徽校名、选文件上传、识别结果、三语介绍、对话 | App、小程序、摄像头实拍 |
| 后端 | Python，本地可启动 | Java、微服务、账号体系 |
| Agent | 真识图、三语介绍、多轮、锁词 | 复杂多智能体框架、写死演示稿 |
| 视觉 | `brand/f_logo.png` + 乐师蓝 `#005CA1` + 乐师金 `#EBBA64` | 通用 AI 紫/黑科技风 |
| 数据 | 文旅术语包（真实词条，待老师审定） | 党政文件、假介绍文案 |

## 3. 本期用户路径

1. 打开网页，顶栏可见乐山师范学院校徽与校名。
2. 手动选择一张图片上传（例如乐山大佛照片）。
3. 后端调用视觉模型 **真实识别**。失败则手选：大佛 / 沫若相关 / 嘉阳小火车 / 校园 / 无法识别。
4. 只装载对应术语包。
5. 模型生成背景介绍，中英日三语并列；专名被锁词层改写/拦截。
6. 用户接着问，同一会话继续；锁定词在后续轮次仍生效。
7. 不得编造未审定事实；不确定就说明并请老师补充。

## 4. 「教案对照」是什么（本期不做）

不是另一个产品，是一条文本演示：把留学生教案贴进网页 → 左边通用翻译会把「沫若」「嘉阳小火车」译飘 → 右边用校本词库锁成审定译法 → 人点黄标 → 导出 PDF。

原评审用它证明「不是套壳翻译」。本期主做识图，这条搁置。

## 5. 智能体四步（识图）

| 步 | 程序 | 人 |
|---|---|---|
| 1 分流 | 图 → 真识别景物 | 认错了可手改 |
| 2 装载 | 只加载对应术语包 | 词条署名审定 |
| 3 锁定 | 精确命中强制采用 | 黄标确认或改译 |
| 4 交付 | 三语介绍 + 多轮对话 | 本期不拍视频 |

覆盖优先级：精确命中 > 黄标近似 > 其余才交给模型。提示词必须带已锁定术语表。

## 6. 术语与视觉资产

术语：先做 **乐山文旅** 包（乐山大佛、沫若、嘉阳小火车等），中英日真实译法，标记待审定。不是「演示用假句」。

视觉文件在 `brand/`，规范见 `brand/视觉规范.md`。校徽来自学校官网，不改画。

## 7. 人机边界

- 人工：范围、术语、锁词规则、提示词定稿、校徽使用
- AI 初稿、人工必改：Web 骨架、识图调用、对话编排、锁词逻辑
- 运行时：真实视觉模型 + 真实介绍/对话；输出必须过锁词层

识图提示词必须含：只识别与乐师/乐山相关景物；不确定交还人手选；介绍不得新增未审定事实。

## 8. 模型连通（已测）

- Base URL、Key、模型名写在仓库根目录 `.env`（已 gitignore，不要提交）。
- 模型：`gpt-5.6-terra`
- 探测脚本：`scripts/test_llm.py`
- 结果：`GET /models` 含该模型；文本 `ping → pong`；识图（校徽 PNG）能回答 YES。

## 9. 明确不做（本期）

视频、教案对照、App、小程序、语音、全量互译、账号系统、党政文件、开放式宣传稿、写死演示包、上复杂 Agent 框架。

参赛 ZIP / 附件 1/2/3 先不动。
